apiVersion: lumis.dev/v1
kind: Project
metadata:
  name: synthetic_housing_regression
spec:
  environment: demo
  memory:
    provider: sqlite
    path: ../.lumis/incidents.db
  reports:
    provider: markdown
    outputDir: ../.lumis/reports
  rules:
    files: [rules.yml]
  model:
    enabled: false
