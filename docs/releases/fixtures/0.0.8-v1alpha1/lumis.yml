apiVersion: lumis.dev/v1alpha1
kind: Project
metadata:
  name: phase-one-upgrade-rehearsal
spec:
  environment: local
  memory:
    provider: sqlite
    path: .lumis/incidents.db
  reports:
    provider: markdown
    outputDir: .lumis/reports
  rules:
    files: [rules.yml]
  model:
    enabled: false
